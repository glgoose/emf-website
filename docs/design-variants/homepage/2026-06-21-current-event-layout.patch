diff --git a/AGENTS.md b/AGENTS.md
index 6e3016a..b560287 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,6 +23,10 @@ for html in Path('dist').rglob('*.html'):
 
 Keep `script-src` hash-only — do **not** add `'unsafe-inline'`. Per CSP3, `'unsafe-inline'` is ignored whenever a hash or nonce is present, so it's dead weight that confuses debugging (browser console complains about it).
 
+## Previewing web changes
+
+When changing pages, layouts, styles, or other user-visible website UI, always offer the user a Codex app preview option so they can view the page from the conversation. Do not only verify through a local browser or screenshots that remain inaccessible to the user.
+
 ### Cloudflare must not inject inline scripts
 
 Hash-based CSP breaks if Cloudflare rewrites the HTML to add a `<script>` with rotating content. Keep these **OFF** in the CF dashboard for zone `ernestmandelfonds.org`:
diff --git a/src/layouts/BaseLayout.astro b/src/layouts/BaseLayout.astro
index be24563..fec51aa 100644
--- a/src/layouts/BaseLayout.astro
+++ b/src/layouts/BaseLayout.astro
@@ -83,7 +83,7 @@ const organizationStructuredData = {
         <!-- Inhoudskolom: navbar at top (homepage) or bottom (all other pages) -->
         <div class="flex flex-col" style="min-height:calc(100vh - 6rem)">
           {isHomepage && <Navbar currentPath={currentPath} atTop={true} />}
-          <main class="flex-1">
+          <main class="flex-1" data-disable-heading-anchors={isHomepage ? 'true' : undefined}>
             <slot />
           </main>
           {!isHomepage && <Navbar currentPath={currentPath} atTop={false} />}
@@ -109,6 +109,7 @@ const organizationStructuredData = {
 
       for (const heading of headings) {
         if (heading.closest('nav,aside')) continue;
+        if (heading.closest('[data-disable-heading-anchors="true"]')) continue;
         if (heading.querySelector('.heading-anchor')) continue;
 
         let { id } = heading;
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 2923e29..350e117 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -1,11 +1,16 @@
 ---
 import { Picture } from 'astro:assets';
 import BaseLayout from '../layouts/BaseLayout.astro';
-import EventCard from '../components/EventCard.astro';
 import NewsletterForm from '../components/NewsletterForm.astro';
 import PillButton from '../components/PillButton.astro';
 import { getCollection } from 'astro:content';
 import symbolImg from '../assets/images/symbolORANGY-RED.png';
+import { formatLongDate, stripHtml } from '../lib/formatDateTime';
+
+const coverImages = import.meta.glob<{ default: ImageMetadata }>(
+  '../assets/images/events/*',
+  { eager: true },
+);
 
 const today = new Date();
 today.setHours(0, 0, 0, 0);
@@ -16,6 +21,33 @@ const upcomingEvents = allEvents
   .sort((a, b) => a.data.date.getTime() - b.data.date.getTime());
 
 const nextEvent = upcomingEvents[0] ?? null;
+
+const allPosts = await getCollection('posts');
+const latestPosts = allPosts
+  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
+  .slice(0, 4);
+
+const formatDate = (date: Date) => date.toLocaleDateString('nl-BE', {
+  day: 'numeric',
+  month: 'long',
+  year: 'numeric',
+});
+
+const formatClockTime = (time: string) => time;
+const formatHomepageTimeRange = (start: string, end?: string | null) =>
+  end ? `${formatClockTime(start)}-${formatClockTime(end)}` : formatClockTime(start);
+
+const nextEventCoverFilename = nextEvent?.data.cover?.split('/').pop();
+const nextEventCover = nextEventCoverFilename
+  ? coverImages[`../assets/images/events/${nextEventCoverFilename}`]?.default
+  : undefined;
+const nextEventTitle = nextEvent ? stripHtml(nextEvent.data.title) : '';
+const nextEventDate = nextEvent ? formatLongDate(nextEvent.data.date) : '';
+const nextEventTime = nextEvent
+  ? formatHomepageTimeRange(nextEvent.data.time, nextEvent.data.end_time)
+  : '';
+const nextEventLocation = nextEvent?.data.location ?? '';
+const nextEventSpeakers = nextEvent?.data.speakers?.map(speaker => speaker.name) ?? [];
 ---
 
 <BaseLayout isHomepage={true}>
@@ -38,22 +70,58 @@ const nextEvent = upcomingEvents[0] ?? null;
   </Fragment>
 
   {nextEvent && (
+    <section class="mb-10">
+      <a href={`/activiteiten/${nextEvent.id}`} class="group block no-underline hover:no-underline focus-visible:no-underline">
+        <article class="grid gap-y-3 sm:grid-cols-[minmax(9.25rem,11.9rem)_1fr] sm:items-start sm:gap-x-0">
+          {nextEventCover && (
+            <Picture
+              src={nextEventCover}
+              alt={nextEventTitle}
+              formats={['avif', 'webp']}
+              widths={[360, 520, 720]}
+              sizes="(min-width: 640px) 11.9rem, 15.4rem"
+              class="home-event-cover w-full max-w-[15.4rem] sm:max-w-none h-[12rem] object-contain object-left-top"
+            />
+          )}
+          <div class="leading-[1.3] sm:-ml-8 sm:flex sm:min-h-[12rem] sm:flex-col sm:justify-between">
+            <div>
+              <p class="mt-0 mb-2 text-[0.65rem] leading-none text-[#555555] font-normal lowercase [font-variant:small-caps] tracking-[0.08em]">volgende activiteit</p>
+              <h3 class="mt-0 mb-0 text-[1.15rem] leading-[1.12] font-normal italic normal-case tracking-normal [font-variant:normal]" set:html={nextEvent.data.title} />
+            </div>
+            <div class="text-[0.88rem] leading-[1.18] space-y-[0.08rem]">
+              <p>{nextEventDate}</p>
+              {nextEventTime && <p>{nextEventTime}</p>}
+              {nextEventLocation && <p>{nextEventLocation}</p>}
+            </div>
+            {nextEventSpeakers.length > 0 && (
+              <div class="text-[0.88rem] leading-[1.18] space-y-[0.08rem]">
+                {nextEventSpeakers.map(speaker => <p>{speaker}</p>)}
+              </div>
+            )}
+          </div>
+        </article>
+      </a>
+    </section>
+  )}
+
+  {latestPosts.length > 0 && (
     <section>
       <h2>
-        Volgende activiteit
+        <a href="/nieuws" class="no-underline hover:no-underline focus-visible:no-underline">Laatste nieuws</a>
       </h2>
-      <EventCard
-        title={nextEvent.data.title}
-        date={nextEvent.data.date}
-        time={nextEvent.data.time}
-        location={nextEvent.data.location ?? undefined}
-        end_time={nextEvent.data.end_time}
-        type={nextEvent.data.type}
-        speakers={nextEvent.data.speakers}
-        cover={nextEvent.data.cover}
-        description={nextEvent.data.description}
-        slug={nextEvent.id}
-      />
+      <div class="grid gap-x-8 gap-y-5 sm:grid-cols-2">
+        {latestPosts.map(post => (
+          <article>
+            <a href={`/nieuws/${post.id}`} class="block no-underline hover:no-underline group">
+              <p class="text-xs text-[#555555] mb-1">{formatDate(post.data.date)}</p>
+              <p class="m-0 text-[1rem] leading-[1.15] font-bold group-hover:underline underline-offset-2">
+                {post.data.title}
+              </p>
+              <p class="mt-2 text-sm leading-[1.35] text-[#333333]">{post.data.summary}</p>
+            </a>
+          </article>
+        ))}
+      </div>
     </section>
   )}
 
@@ -62,3 +130,15 @@ const nextEvent = upcomingEvents[0] ?? null;
     <NewsletterForm />
   </div>
 </BaseLayout>
+
+<style>
+  :global(.home-event-cover) {
+    filter: grayscale(1) brightness(0.97);
+    transition: filter 180ms ease;
+  }
+
+  .group:hover :global(.home-event-cover),
+  .group:focus-visible :global(.home-event-cover) {
+    filter: brightness(0.97);
+  }
+</style>
