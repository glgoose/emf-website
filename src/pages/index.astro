---
import BaseLayout from '../layouts/BaseLayout.astro';
import EventCard from '../components/EventCard.astro';
import NewsletterForm from '../components/NewsletterForm.astro';
import PillButton from '../components/PillButton.astro';
import { getCollection } from 'astro:content';

const today = new Date();
today.setHours(0, 0, 0, 0);

const allEvents = await getCollection('events');
const upcomingEvents = allEvents
  .filter(e => e.data.date >= today)
  .sort((a, b) => a.data.date.getTime() - b.data.date.getTime());

const nextEvent = upcomingEvents[0] ?? null;
---

<BaseLayout isHomepage={true}>
  <Fragment slot="aside">
    <a href="/" aria-label="Ernest Mandelfonds – startpagina" class="flex flex-row items-center gap-4 lg:flex-col lg:items-end hover:no-underline">
      <p class="whitespace-nowrap italic text-[1.5rem] leading-[1.25] lg:mt-3 lowercase text-[#f0573e] rotate-[-10deg]">Ernest Mandelfonds</p>
      <img
        src="/images/symbolORANGY-RED.png"
        alt="Ernest Mandelfonds"
        class="block max-w-[50px] sm:max-w-[65px] lg:max-w-[80px]"
      />
    </a>
  </Fragment>

  {nextEvent && (
    <section>
      <h2>
        Volgende activiteit
      </h2>
      <EventCard
        title={nextEvent.data.title}
        date={nextEvent.data.date}
        time={nextEvent.data.time}
        location={nextEvent.data.location ?? undefined}
        end_time={nextEvent.data.end_time}
        type={nextEvent.data.type}
        speakers={nextEvent.data.speakers}
        cover={nextEvent.data.cover}
        description={nextEvent.data.description}
        slug={nextEvent.id}
      />
    </section>
  )}

  <div class="flex flex-col items-start sm:flex-row sm:items-center sm:justify-between gap-4 mt-10">
    <PillButton href="/steun-ons" class="shrink-0">Steun ons</PillButton>
    <NewsletterForm />
  </div>
</BaseLayout>
